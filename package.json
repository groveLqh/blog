{
  "name": "yinshui-blog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "dev": "node scripts/build-site.mjs --watch",
    "serve": "python3 -m http.server 4173 --directory dist"
  },
  "engines": {
    "node": ">=20"
  }
}
